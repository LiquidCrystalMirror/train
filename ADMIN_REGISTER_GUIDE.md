# 管理员注册页面使用说明

## ⚠️ 重要提示

**不要直接双击打开HTML文件！**

必须通过Spring Boot服务器访问，否则会出现跨域错误。

---

## ✅ 正确的使用方式

### 步骤1：启动后端服务

```bash
# 进入spring目录
cd C:\Users\Administrator\IdeaProjects\train\spring

# 启动Spring Boot应用
mvn spring-boot:run

# 或者在IDE中运行 TicketApplication.java
```

确保看到类似以下输出：
```
Started TicketApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)
```

---

### 步骤2：通过浏览器访问

在浏览器地址栏输入：
```
http://localhost:8080/admin-register.html
```

**注意**：
- ✅ 正确：`http://localhost:8080/admin-register.html`
- ❌ 错误：直接双击打开文件（file://协议）

---

### 步骤3：填写管理员信息

表单字段：
- **用户名**：2-20个字符
- **真实姓名**：必填
- **身份证号**：15或18位
- **手机号**：11位，以1开头
- **密码**：6-20个字符
- **确认密码**：必须与密码一致

---

### 步骤4：提交注册

点击"注册管理员"按钮：

**成功**：
- 显示绿色成功消息
- 3秒后自动跳转到登录页

**失败**：
- 显示红色错误消息
- 根据提示修正问题

---

## ❌ 常见错误及解决

### 错误1：Failed to fetch

**原因**：直接用浏览器打开HTML文件（file://协议）

**解决**：
1. 关闭当前页面
2. 确保后端已启动
3. 访问 `http://localhost:8080/admin-register.html`

---

### 错误2：CORS跨域错误

**原因**：前端和后端不在同一个域名/端口

**解决**：
- 后端已配置CORS允许跨域
- 确保通过 `http://localhost:8080` 访问

---

### 错误3：404 Not Found

**原因**：后端服务未启动或路径错误

**解决**：
1. 检查后端是否启动
2. 确认访问地址是 `http://localhost:8080/admin-register.html`
3. 查看后端控制台是否有错误日志

---

### 错误4：用户名已存在

**原因**：该用户名已被注册

**解决**：更换一个唯一的用户名

---

## 🔍 调试技巧

### 1. 检查后端是否启动

浏览器访问：
```
http://localhost:8080/api/v1/g/users
```

如果返回JSON数据，说明后端正常。

---

### 2. 查看浏览器控制台

按 `F12` 打开开发者工具：
- **Console** 标签：查看JavaScript错误
- **Network** 标签：查看API请求详情

---

### 3. 查看后端日志

在IDE或终端中查看Spring Boot的输出日志，可以看到：
- 接收到的请求
- 数据库操作
- 错误堆栈信息

---

## 📝 测试流程

### 完整测试示例

1. **启动后端**
   ```bash
   cd spring
   mvn spring-boot:run
   ```

2. **访问注册页面**
   ```
   http://localhost:8080/admin-register.html
   ```

3. **填写信息**
   ```
   用户名：admin001
   真实姓名：系统管理员
   身份证号：110101199001011234
   手机号：13800138000
   密码：admin123
   确认密码：admin123
   ```

4. **提交注册**
   - 看到绿色成功消息
   - 等待3秒自动跳转

5. **验证结果**
   - 使用新账号登录
   - 登录后查看用户角色应为"管理员"

---

## 🔐 安全建议

1. **内网访问**：建议将此页面部署在内网环境
2. **访问控制**：生产环境应添加额外的认证机制（如邀请码）
3. **删除页面**：创建完初始管理员后，可以考虑删除此HTML文件
4. **修改密码**：首次登录后立即修改密码

---

## 📞 需要帮助？

如果遇到问题，请检查：

1. ✅ 后端服务是否启动
2. ✅ 访问地址是否正确（http://localhost:8080/admin-register.html）
3. ✅ 数据库连接是否正常
4. ✅ 浏览器控制台是否有错误
5. ✅ 后端日志是否有异常

---

**最后更新**: 2026-05-24
